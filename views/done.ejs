<% include partials/header %>
  <body>
    <div class="wrapper">
       <% include partials/nav %>
          <div class="container">
            <div class="row">
              <div class="col-md-6">
                <h1><%= tasks.title %></h1>
                <p class="lead mt-3"><%= tasks.details %></p>
              </div>
              <div class="col-md-6">
                  <a href="/cats/<%= task._id %>/edit" class="btn btn-lg btn-primary btn-block">Edit</a>
              </div>
            </div>
            <div class="col-md-6">
                <form method="POST" action="/tasks/<%= tasks._id %>?_method=DELETE">
                  <button class="btn btn-warning btn-block btn-lg">Delete</button>
                </form>
              </div> 
          </div>
      </div>
    <% include partials/footer%>